export interface FilterCheckboxInterfaces {
  id: number;
  value: string;
  checked: boolean;
}

export interface SearchOptions {
  text: string
  authors: string[];
  genres: string[],
  series: string[],
}
